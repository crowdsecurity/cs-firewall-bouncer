[pytest]
addopts =
    --pdbcls=IPython.terminal.debugger:Pdb
    --ignore=rpm
    --strict-markers
    -m "not (deb_build or deb_install or rpm or scripts or iptables or nftables)"
markers:
    deb_build: tests for .deb packaging (non-root)
    deb_install: tests for .deb packaging (root)
    rpm: tests for .rpm packaging
    scripts: test installation scripts (root)
    iptables: tests for mode=iptables (root)
    nftables: tests for mode=nftables (root)
env_files =
    .env
    default.env
